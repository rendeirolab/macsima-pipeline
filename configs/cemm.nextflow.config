singularity {
    enabled    = true
    autoMounts = true
}

process {
    resourceLimits = [
        memory: 750.GB,
        cpus: 200,
        time: 30.d
    ]
    executor = 'slurm'
    queue    = 'tinyq'
    clusterOptions = '--qos=tinyq'
}
